apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logto-postgres-pvc
  namespace: devver-logto
spec:
  storageClassName: nfs-devver-prod
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi